{
  "name": "mcp-broker",
  "version": "1.1.0",
  "description": "MCP tool-routing broker — tool providers connect outbound and publish tools, MCP clients consume them transparently",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "repl": "node repl.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "express": "^4.21.0",
    "ws": "^8.16.0"
  }
}
